[project]
name = "shnx-shell"
version = "0.1.0"
description = "A custom Python desktop shell for Hyprland"
requires-python = ">=3.12"
dependencies = [
    "PyGObject",
]

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
